export interface NavLink {
  path: string;
  label: string;
}

export interface User {
  id: string;
  name: string;
  email: string;
  // Add more user fields as needed
}

// Add more types as development progresses
